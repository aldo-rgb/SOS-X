# Backend local para desarrollo
VITE_API_URL=http://localhost:3001

# URL pública del backend para mostrar webhooks (siempre apunta a producción
# porque servicios externos como Facturama no pueden llegar a localhost).
VITE_PUBLIC_BACKEND_URL=https://sos-x-production.up.railway.app
